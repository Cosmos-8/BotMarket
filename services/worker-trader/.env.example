# ============================================================================
# Worker Trader Environment Configuration
# ============================================================================

# Database connection
DATABASE_URL=postgresql://user:password@localhost:5432/botmarket

# Redis connection
REDIS_URL=redis://localhost:6379

# Bot key encryption secret (must match API)
BOT_KEY_ENCRYPTION_SECRET=your-32-char-secret-key-here

# ============================================================================
# Trading Mode Configuration
# ============================================================================

# TRADING_MODE: Controls how orders are executed
#   - mock    : (DEFAULT) Orders are simulated locally, no real trades
#   - gamma   : Orders are signed and sent to Polymarket Gamma (testnet)
#   - mainnet : Orders are signed and sent to Polymarket mainnet (REAL MONEY)
TRADING_MODE=mock

# ============================================================================
# Polymarket Credentials (Required for gamma/mainnet modes)
# ============================================================================

# Private key for signing Polymarket orders (Polygon wallet)
# NEVER commit this to version control!
# Only required if TRADING_MODE=gamma or TRADING_MODE=mainnet
POLYMARKET_PRIVATE_KEY=

# Wallet address corresponding to the private key
POLYMARKET_WALLET_ADDRESS=

# ============================================================================
# Polymarket API URLs
# ============================================================================

# Gamma API (market data) - used for market discovery
POLYMARKET_GAMMA_API=https://gamma-api.polymarket.com

# CLOB API base URL - automatically set based on TRADING_MODE if not specified
# Gamma testnet: https://clob.polymarket.com  (same endpoint, different behavior)
# Mainnet: https://clob.polymarket.com
POLYMARKET_CLOB_API=https://clob.polymarket.com

# ============================================================================
# Safety Caps (Enforced for gamma/mainnet modes)
# ============================================================================

# Maximum size for a single trade in USD
# Trades exceeding this limit will be skipped with a warning
MAX_TRADE_SIZE_USD=1

# Maximum total notional traded per day in USD (rolling 24h or since midnight)
# Once this limit is reached, all subsequent trades will be skipped
MAX_DAILY_NOTIONAL_USD=10

# ============================================================================
# Legacy Configuration (Deprecated, use TRADING_MODE instead)
# ============================================================================

# ENABLE_LIVE_TRADING is deprecated but still supported for backwards compat
# If set to 'true', acts as TRADING_MODE=mainnet (use with extreme caution)
# ENABLE_LIVE_TRADING=false

# Legacy API credentials (now prefer POLYMARKET_PRIVATE_KEY for EIP-712 signing)
# POLYMARKET_API_KEY=
# POLYMARKET_API_SECRET=
# POLYMARKET_API_PASSPHRASE=
